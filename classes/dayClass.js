export class Day {
    constructor(id, day, date, summary, maxTemp, minTemp, windSpeed, icon) {
        this.id = id,
        this.day = day,
        this.date = date,
        this.summary = summary,
        this.maxTemp = maxTemp,
        this.minTemp = minTemp,
        this.windSpeed = windSpeed,
        this.icon = icon
    }

    
}